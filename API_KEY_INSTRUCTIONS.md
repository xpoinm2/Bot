# 🔑 Инструкции по работе с API ключом OpenAI

## Текущая настройка

API ключ OpenAI указан **в файле run_bot.bat**:

```batch
set "OPENAI_API_KEY=sk-proj-ВАШ_КЛЮЧ_ЗДЕСЬ"
```

## Перед отправкой на GitHub

### Шаг 1: Замените реальный ключ на заглушку
```python
# 🔴 ЗАМЕНИТЕ ПЕРЕД КОММИТОМ НА GITHUB!
OPENAI_API_KEY = "your-openai-api-key-here"
```

### Шаг 2: Сделайте коммит
```bash
git add .
git commit -m "Replace API key with placeholder before GitHub"
git push
```

### Шаг 3: Верните реальный ключ обратно
```python
OPENAI_API_KEY = "sk-proj-ВАШ_НАСТОЯЩИЙ_КЛЮЧ"
```

## Как получить API ключ

1. Зайдите на [platform.openai.com](https://platform.openai.com)
2. Создайте аккаунт или войдите
3. Перейдите в **API Keys**
4. Нажмите **"Create new secret key"**
5. Скопируйте ключ и вставьте в код

## Проверка работы

После замены ключа протестируйте бота:
```bash
python tg_manager_bot_dynamic.py
```

Если бот запускается без ошибок - ключ работает корректно.

## Безопасность

- ❌ **НЕ** коммитьте реальные API ключи на GitHub
- ❌ **НЕ** делитесь ключами с другими
- ✅ **Всегда** заменяйте на заглушку перед пушем
- ✅ **Регулярно** проверяйте ключи на [platform.openai.com](https://platform.openai.com)

## Альтернативный способ (.env файл)

Если хотите использовать .env файл вместо прямого указания в коде:

1. Создайте файл `.env`:
```
OPENAI_API_KEY=ваш_ключ_здесь
```

2. Добавьте загрузку в код:
```python
from dotenv import load_dotenv
load_dotenv()
```

3. Добавьте `python-dotenv` в `requirements.txt`

Но текущий способ (прямое указание в коде) проще для приватного использования.
